tpitable = function(nmax, cohort, w, a1 = NULL, b1 = NULL, a2 = NULL, b2 = NULL)
{
  if(isTRUE(namx < 3))
  {
    if(isTRUE(nmax <= 0))
    {
      stop("Number of patients must be positive and at least 3")
    }
    else
    {
      stop("Number of patients must be atleast 3")
    }
  }

}
